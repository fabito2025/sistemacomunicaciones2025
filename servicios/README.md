## Instrucciones para levantar el proyecto

1. Crear entorno virtual: 
esta es la rura que quede afuera del servicios
C:\nombre_del _peoyecto> python -m venv env


python -m venv env

env\Scripts\activate

2. Activar las dependencias

pip install -r requirements.txt

python manage.py runserver

Esta es la ruta
(env) PS C:\pro_comunicaciones\servicios> pip install -r requirements.txt 


3. En caso se que no sirva apliacar el comando manual

python -m venv env

env\Scripts\activate

python -m pip install django
pip install django-widget-tweaks
